/**
 * Settings tab — renders the full settings screen.
 */

import { SettingsScreen } from '@/components/settings/settings-screen';

export default function SettingsTab() {
  return <SettingsScreen />;
}
