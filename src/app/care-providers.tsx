/**
 * Care & Providers route.
 *
 * Keeps provider-facing simulation tools in the normal Care & Providers area
 * instead of Advanced Developer Settings while preserving their existing
 * patient-record mutation paths.
 */
export { CareProvidersScreen as default } from "@/components/settings/settings-screen";
